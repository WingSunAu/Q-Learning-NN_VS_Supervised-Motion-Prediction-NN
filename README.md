# Q-Learning-NN_VS_Supervised-Motion-Prediction-NN
The control system includes a neural network trained using Deep Q-Learning to estimate action values and select optimal actions. The Motion Prediction Neural Network is trained with supervised learning to select optimal actions based on object physics.
